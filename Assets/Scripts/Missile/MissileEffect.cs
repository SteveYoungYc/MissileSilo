using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MissileEffect : MonoBehaviour
{
    public GameObject bigExplodeEffectPrefab;
    // Start is called before the first frame update
    void Start()
    {
        //Instantiate(effectPrefab, transform.position, Quaternion.identity).transform.parent = gameObject.transform;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
